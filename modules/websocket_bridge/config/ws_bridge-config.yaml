ws_server:
  listening_port: 8765
  address: "0.0.0.0"
  client_topic_whitelist:
    - ".*"
  supported_encodings:
    - "json"
    - "protobuf"
  use_compression: true

ipc:
  spin_rate_hz: 0.5
  topic_whitelist:
    - ".*"
  topic_blacklist: []
  service_whitelist:
    - ".*"
  service_blacklist: []
